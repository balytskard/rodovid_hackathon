# 🚀 ІНСТРУКЦІЯ ЗАПУСКУ

## Крок 1: Запустити Backend

**Відкрити НОВИЙ PowerShell (Термінал 1):**
```powershell
cd X:\MyUserFiles\Desktop\diia\MERGE_WORKSPACE\RODVID_FINAL\backend
python main.py
```

Або подвійний клік на: `START_BACKEND.bat`

**Дочекатися:**
```
✅ Neo4j готовий!
✅ Validators з TimeResolver готові!
INFO:     Uvicorn running on http://0.0.0.0:8000
```

---

## Крок 2: Створити Тестову Родину

**В ІНШОМУ PowerShell (Термінал 2):**
```powershell
cd X:\MyUserFiles\Desktop\diia\MERGE_WORKSPACE\RODVID_FINAL\backend
python test_complex_family.py
```

**Очікуваний результат:**
- ✅ 22 особи створені
- ✅ 30 зв'язків створені
- 4 покоління: прадіди → діди → батьки → діти

---

## Крок 3: Відкрити Frontend

**Відкрити браузер:**
```
http://localhost:3000
```

**Натиснути:** `Ctrl + Shift + R` (hard refresh)

**Очікується:**
- Дерево з 22 вузлами
- 4 покоління
- Правильні зв'язки

---

## 🔧 Якщо Backend НЕ запускається:

**Перевірити Neo4j:**
```powershell
# Neo4j Desktop: переконайтесь що база запущена
# Або перевірте Docker:
docker ps
```

**Перевірити .env:**
```
NEO4J_URI=bolt://127.0.0.1:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=rodovid123
```

---

## 📝 Ghost Nodes - Як Працює

**Ghost node** = видалена особа, але з дітьми:
- ✅ Зберігає всі зв'язки (PARENT_OF, SPOUSE, SIBLING)
- ✅ Може мати дітей, шлюби, братів/сестер
- ✅ Відображається сірим кольором
- ⚠️ Зашифровані дані видалені
- 📝 Автоматичне ім'я: "Прадід", "Прабаба", "Предок (чол.)" тощо

**Створення Ghost:**
```javascript
// При видаленні в UI:
DELETE /api/v1/person/{id}?user_id=user_1

// Backend перевіряє:
if (має нащадків) {
  → is_deleted = true
  → ghost_name = автоматична назва
  → зв'язки залишаються
} else {
  → DETACH DELETE (повне видалення)
}
```

---

## 🎯 Поточний Стан

✅ Backend готовий (запустити)
✅ Frontend готовий
✅ БД очищена (0 вузлів user_1)
✅ Тестовий скрипт готовий
✅ Ghost nodes підтримують всі зв'язки

**Залишилось:**
1. Запустити backend (`python main.py`)
2. Запустити тест (`python test_complex_family.py`)
3. Відкрити frontend (http://localhost:3000)
