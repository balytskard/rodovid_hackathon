# ⚡ Швидкий старт

Запустіть проект за 5 хвилин!

## 1️⃣ Перевірте передумови

```bash
# Python 3.10+
python --version

# Node.js 16+
node --version
npm --version

# Neo4j (має бути запущений)
# Перевірте: http://localhost:7474
```

## 2️⃣ Налаштуйте Neo4j

**Опція A: Neo4j Desktop** (рекомендовано)
1. Завантажте з [neo4j.com/download](https://neo4j.com/download/)
2. Створіть новий проект
3. Створіть базу з паролем `rodovid123`
4. Запустіть базу

**Опція B: Docker**
```bash
docker run -d \
  --name neo4j \
  -p 7474:7474 -p 7687:7687 \
  -e NEO4J_AUTH=neo4j/rodovid123 \
  neo4j:5-community
```

## 3️⃣ Налаштуйте Backend

```bash
cd backend

# Створіть .env файл
echo NEO4J_URI=bolt://127.0.0.1:7687 > .env
echo NEO4J_USER=neo4j >> .env
echo NEO4J_PASSWORD=rodovid123 >> .env

# Встановіть залежності
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Linux/Mac
pip install -r requirements.txt

# Запустіть backend
python main.py
```

Відкрийте http://localhost:8000/docs - має показатись Swagger UI

## 4️⃣ Налаштуйте Frontend

```bash
cd frontend

# Встановіть залежності
npm install

# Запустіть frontend
npm start
```

Відкрийте http://localhost:3000 - має показатись додаток

## 5️⃣ Перший запуск

1. **Додайте першу персону:**
   - Клік на кнопку "➕ Додати персону"
   - Заповніть: ПІБ, рік народження
   - Виберіть зв'язок: "Дитина" (для першого запуску створюється Root User)
   - Клік "Зберегти"

2. **Перегляньте дерево:**
   - Має з'явитись кружечок з ініціалами
   - Клік на кружечок → відкриється модалка з деталями
   - Можна зумити (колесо миші) та перетягувати (drag)

3. **Додайте батька/матір:**
   - Клік "➕ Додати персону"
   - Заповніть дані
   - Виберіть зв'язок: "Батько" або "Мати"
   - Виберіть до кого прив'язати (dropdown list)
   - Зберегти

4. **Перемкніть шифрування:**
   - Клік на кнопку "🔒 Зашифровано" вгорі
   - Тепер бачите сирі дані з бази (зашифровані)
   - Клік знову → розшифровані дані

## 🎯 Що далі?

- Прочитайте [README.md](README.md) для повної інформації
- Перегляньте [docs/API.md](docs/API.md) для API документації
- Вивчіть [docs/SECURITY.md](docs/SECURITY.md) для розуміння безпеки

## ⚠️ Типові проблеми

**"Cannot connect to Neo4j"**
- Перевірте чи запущений Neo4j: http://localhost:7474
- Перевірте пароль в .env файлі

**"Port 8000 already in use"**
- Закрийте інший процес що використовує порт 8000
- Або змініте порт в `backend/main.py` (line: `uvicorn.run`)

**Frontend не підключається до backend**
- Перевірте чи працює backend: http://localhost:8000
- Перевірте CORS налаштування в `backend/main.py`

**Дерево не відображається**
- Відкрийте DevTools (F12) → Console
- Шукайте червоні помилки
- Перезавантажте сторінку (Ctrl+Shift+R)

## 🆘 Потрібна допомога?

Створіть Issue на GitHub з:
- Версією Python/Node.js
- Логами з backend терміналу
- Помилками з браузер console (F12)
